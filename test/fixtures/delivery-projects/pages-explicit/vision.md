# Explicit Pages Exception

Use Cloudflare Pages for this project because the product must be deployed as
Cloudflare Pages with Pages Functions.

This fixture is intentionally the exception case; normal Worker fixtures should
not select Pages.
