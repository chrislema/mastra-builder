# Talking Head Builder

Talking Head Builder turns weekly bookmarks into one ready-to-record
talking-head transcript.

The creator uploads an `audience_segments` profile and a `voice_profile`.
The Worker uses Workers AI for transcript generation and stores completed runs
so the latest transcript can be retrieved later.
