# D1 Runs API

Build a Cloudflare Worker that stores submitted runs in Cloudflare D1.

The project needs SQL migrations, a repository layer, and API routes for
creating a run and reading the latest run.
