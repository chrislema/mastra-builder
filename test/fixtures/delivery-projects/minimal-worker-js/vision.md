# Minimal Worker JS

Build a tiny standalone Cloudflare Worker health-check API.

Use plain JavaScript. Do not use React, Vite, Cloudflare Pages, a database, or
object storage. The Worker should be deployable with Wrangler.
