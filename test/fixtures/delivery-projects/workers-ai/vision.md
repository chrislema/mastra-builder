# Workers AI Summary

Build a Cloudflare Worker that summarizes user-provided notes with Workers AI.

The Worker must use an active AI binding named `AI` through `env.AI`. Use fake
provider inputs in tests and do not call external provider APIs during unit
tests.
