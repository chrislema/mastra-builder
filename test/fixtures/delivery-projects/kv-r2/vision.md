# KV And R2 Assets

Build a Cloudflare Worker asset manager.

Use Cloudflare KV for short-lived asset metadata and Cloudflare R2 for uploaded
binary artifacts. The public UI should stay vanilla HTML, CSS, and JavaScript.
