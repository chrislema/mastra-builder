# TypeScript Public UI

Build a TypeScript Cloudflare Worker with a vanilla public HTML, CSS, and
JavaScript interface. The app should expose a small JSON API and serve static
assets from the Worker.

Do not use React, Vite, Next.js, Pages, or a frontend build step.
